# Copy to .env — never commit .env
IMAGE_PROVIDER=litellm
LITELLM_BASE_URL=http://localhost:4000
LITELLM_API_KEY=
LITELLM_IMAGE_MODEL=
# LITELLM_IMAGE_SIZE=1024x1024
# Chat model used to name invented buildings (same LiteLLM gateway)
LITELLM_CHAT_MODEL=

# Used when IMAGE_PROVIDER=pixellab
PIXELLAB_API_KEY=
